(function() {
    'use strict';

    angular
        .module('app.resume')
        .controller('Resume', Resume);

    //Resume.$inject = [];

    function Resume($timeout, config) {
        //send dataservice request
    }
})();
